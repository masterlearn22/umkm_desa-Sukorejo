# Workspace Rules

Whenever you modify files or make changes to this project, ALWAYS follow up by running `git add .`, `git commit` (with a descriptive message), `git push`, and then `npm run deploy` (use `cmd.exe /c "npm run deploy"` on Windows). This ensures every change is immediately deployed to the live site as requested by the user.
